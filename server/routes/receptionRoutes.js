import Router from 'express';
// import asyncErrorHandler from '../helpers/asyncErrorHandler';
// import isAdmin from '../middlewares/isAdmin';
// import paginate from '../middlewares/paginateMiddleware';

const router = Router();

// router.post('/leagues', tokenValidation, isAdmin, imageLeagueUploader.fields([{ name: 'image', maxCount: 1 }]), asyncErrorHandler(LeagueController.addLeague));
// router.patch('/leagues/:id', tokenValidation, isAdmin, imageLeagueUploader.fields([{ name: 'image', maxCount: 1 }]), asyncErrorHandler(LeagueController.changeLeague));
// router.get('/leagues', tokenValidation, LeagueController.viewLeagues, paginate.paginatedRetrievedData);
// router.get('/leagues/:id', tokenValidation, isAdmin, asyncErrorHandler(LeagueController.viewLeague));
// router.delete('/leagues/:id', tokenValidation, isAdmin, asyncErrorHandler(LeagueController.removeLeague));

export default router;
